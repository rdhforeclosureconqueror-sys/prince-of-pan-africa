import React,{useEffect,useState}from"react";import{Link,useParams}from"react-router-dom";import{getSocietyDirectory}from"../api/societyBuilder";import"../styles/societyBuilder.css";
export default function SocietyDirectoryPage(){const{societyId}=useParams();const[data,setData]=useState(null);const[error,setError]=useState("");useEffect(()=>{getSocietyDirectory(societyId).then(setData).catch(e=>setError(e.message));},[societyId]);if(!data)return <main className="society-builder-shell"><p>Loading Society Directory...</p>{error&&<p className="society-warning">{error}</p>}</main>;return <main className="society-builder-shell"><p className="society-kicker">Society Directory</p><h1>{data.society.name}</h1><p>Members are grouped by contribution category. Private needs are never shown here.</p>{error&&<p className="society-warning">{error}</p>}<section className="society-grid">{data.directory.groups.map(g=><article className="society-card" key={g.category}><h2>{g.category}</h2>{g.members.map(m=><div className="society-result" key={m.id}><strong>{m.display_name}</strong><p>{m.headline}</p><p>{m.primary_contribution} · {m.availability}</p></div>)}</article>)}{!data.directory.groups.length&&<article className="society-card"><h2>No members listed yet</h2><p>Ask members to answer: How will you help build this institution?</p></article>}</section><Link className="society-btn" to={`/societies/${societyId}/profile/edit`}>Add My Contribution</Link></main>}
